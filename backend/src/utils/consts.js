const StockStatus = {
  RED: 'red',
  YELLOW: 'yellow',
  GREEN: 'green',
};

const MEDICATION_UNITS = ['tablets', 'capsules', 'vials', 'ampoules', 'mg', 'ml', 'units', 'boxes'];

const WITHDRAWAL = 'withdrawal';

const RESTOCK = 'restock';

module.exports = { StockStatus, MEDICATION_UNITS, WITHDRAWAL, RESTOCK };
