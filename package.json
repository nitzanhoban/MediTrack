{
  "name": "meditrack",
  "private": true,
  "version": "1.0.0",
  "description": "Smart medication inventory management and shortage-prediction system for hospitals.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "test:backend": "npm test --workspace=backend"
  }
}
